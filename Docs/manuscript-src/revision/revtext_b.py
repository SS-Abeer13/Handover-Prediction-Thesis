# -*- coding: utf-8 -*-
"""Revised manuscript text, part B: Chapter 4 (modelling), Chapter 5, 6, 7."""
from revtext_a import DELETE


def part_b(N):
    return {
# ---------------------------------------------------------------- CH 4
333: ("with K = 5 and horizons of 0.5, 1, 2, 3 and 5 seconds. Samples for which the drive ends "
     "before the next handover occurs are right-censored: the event is known not to have occurred "
     "within the observed window, but its eventual time is unknown. The formulation that follows "
     "handles such samples, and Section 4.4 gives the likelihood that does so. It should be said "
     "immediately, however, that on this dataset the machinery is exercised by nothing: the row "
     "filter of Section 3.7 keeps only rows carrying a full five-second follow-up window inside "
     "their own campaign, so no retained row is censored inside the horizon grid. The censoring "
     "treatment is therefore developed because the formulation is not specific to these four "
     "campaigns — a longer horizon grid or a shorter session would make it bind — and it is not "
     "counted anywhere in this thesis as an advantage the measurements demonstrate."),
334: ("Table 4.1 reports the resulting positive-class prevalence at each horizon, computed on the row "
     "set used for every experiment in this thesis: rows with a full five-second follow-up window "
     "inside their own campaign and outside the two-second post-handover blank. Using one row set for "
     "all five horizons is what makes the prevalence table of Table 4.1 and the hazard table of "
     "Table 4.2 two views of the same numbers."),
336: ("Table 4.1 is the reason accuracy is not reported anywhere in this thesis. At the one-second "
     f"horizon a model that never predicts a handover already scores {1 - N['prev1']:.1%}, which "
     "exceeds several of the headline accuracies reported in the audited literature of Section 2.3."),
339: ("The defect. The K classifiers are fitted independently and are therefore free to produce "
     "estimates that violate the ordering implied by their own definitions. Because the event "
     "{ T_t ≤ h1 } is contained in { T_t ≤ h2 } whenever h1 < h2, the probabilities must satisfy "
     "F1 ≤ F2 ≤ … ≤ FK at every sample. Independent fits do not enforce this, and Section 5.4 "
     f"measures how often they violate it: on {N['viol_indep']:.1%} of rows."),
341: ("Why post-hoc repair is not free, and where it is. The defect can be repaired after the fact by "
     "projecting the K outputs onto the monotone cone — a per-row cumulative maximum, or the "
     "pool-adjacent-violators projection — or by calibrating each horizon separately with the "
     f"isotonic regression of Zadrozny and Elkan [{N['ref_zadrozny']}], which is the standard "
     "non-parametric alternative to the temperature scaling of Guo et al. "
     f"[{N['ref_guo']}] and is preferred here because unconstrained temperature scaling fitted separately "
     "per horizon does not preserve cross-horizon ordering, whereas isotonic calibration non-parametrically "
     "aligns probability-frequency agreement directly. All three repairs are implemented and "
     "reported in Section 5.4 as controls, and the first "
     "two need no held-out split and reach zero violations by construction, exactly as the hazard "
     "formulation does. What remains of the case for the hazard formulation, after the concessions "
     "this section and Section 5.4 make, is narrow and should be stated as such: it achieves "
     "coherence from one fit rather than five, and it calibrates better than the projected controls "
     "at the horizons where the output is usable as a probability. Two advantages claimed for it "
     "elsewhere in the literature do not apply to this data and are withdrawn here. It does not rank "
     "better — Section 5.4 measures the hazard arm slightly below the independent control, well "
     "inside the per-campaign spread. And it does not inherit any benefit from the censoring "
     "treatment of Section 4.4, because no row in this dataset is censored inside the horizon grid; "
     "the censoring-aware likelihood is correct and general, and on these four campaigns it is "
     "inert. That leaves a convenience argument and a calibration argument, and the abstract and "
     "Chapter 7 now say so."),
355: ("Figure 4.1 illustrates the two formulations side by side."),
361: ("On this dataset the expansion takes the "
     f"{N['rows_usable']:,} usable rows to {N['long_rows']:,} (sample, bin) pairs. Because every "
     "retained row carries a full five-second follow-up window inside its own campaign, no row is "
     "censored inside the horizon grid, and the measured per-bin hazards chain exactly to the "
     "prevalences of Table 4.1 by Equation (4.4). Table 4.2 reports both, side by side, as an "
     "arithmetic check on the implementation."),
# 4.5 reweighting: fix the wrong compounding claim, preserving Equation (4.7) at LM[366]
367: ("Retraining with class weighting alters the gradient balance during optimization and distorts the "
     "calibrated probability scale. Furthermore, combining rescaled per-bin hazards across horizons does "
     "not generally preserve cumulative incidence rankings across samples. Writing the reweighted hazards as "
     "λ̃_k ≈ w λ_k for small λ_k, the survival function is transformed as"),
368: "S̃_k = Π_j (1 − w λ_j) ≈ (S_k)^w,   so   F̃_k ≈ 1 − (1 − F_k)^w\t(4.8)",
369: ("Equation (4.8) replaces a compounding claim made in an earlier draft of this work, which stated "
     "that a per-bin multiplicative error ε grows as (1+ε)^k with the horizon. That is not what the "
     "product-limit identity does: for small hazards the relative distortion of F_k is bounded by w "
     "and is largest at the shortest horizon, while the absolute distortion grows with F_k and is "
     "therefore largest at the longest. Retraining under class weights destroys probability calibration "
     "without guaranteeing rank preservation once hazards are multiplied across bins; for these reasons, "
     "both the hazard arm and the independent-classifier control arm are fitted unweighted, and the fairness "
     "of the comparison between them is a secondary benefit."),
# 4.6 features
371: ("Design. The feature vector at the prediction instant t has three blocks and "
     f"{N['n_features']} columns in total: {N['n_feat_radio']} radio, {N['n_feat_mobility']} mobility "
     f"and {N['n_feat_history']} history. The radio and mobility blocks are built from the one-hertz "
     "export; the history block is built from the decoded signalling. A further "
     f"{N['n_feat_signalling']} signalling and configuration columns are built but withheld from every "
     "learner outside the ablation of Section 5.8, so the design matrix on disk has "
     f"{N['n_feat_matrix']} columns while the main feature set has {N['n_features']}; Table 4.3 gives "
     "the reconciliation, and every count quoted anywhere in this thesis is generated from it. Two "
     "rules govern the construction of the blocks, and the first is the single most consequential "
     "decision in this thesis."),
372: ["Alignment. An export row stamped t summarises an interval that ends after t, not before it: "
      f"Section 5.1 shows that in {N['align_t0']:.0%} of handovers the row stamped inside the second "
      "before the handover command already carries the post-handover serving cell, against a "
      f"background rate of {N['align_iso_tm3']:.0%} three rows earlier. Every export-derived feature "
      "is therefore lagged by one row, so that a prediction made at t uses only the row closing at or "
      "before t. Nothing else in this thesis changes as much as this does.",
      "History from the event clock, not from the export. Time since the previous handover command, "
      "the number of commands in the previous 30 and 60 seconds, and whether the previous command was "
      "a return are computed from the decoded signalling, whose timestamps are exact to the "
      "millisecond, and over the whole continuous session rather than within a 180-second block. "
      "Computing them per block left a third of every block with a truncated history, and computing "
      "dwell time from the export's serving-cell column imports the alignment error described above: "
      f"that single substitution moves the dwell feature's own AUROC from {N['lag0_dwell_auroc']:.3f} "
      f"to {N['dwell_auroc']:.3f}."],
373: ("Every rolling window is strictly backward-looking and closes at the lagged row, and window "
     "computation is performed per campaign so that no window spans a session boundary. Scalers, "
     "imputation values, thresholds and calibrators are fitted inside the training partition of each "
     "fold only."),
374: ("The signalling block is reported as an ablation rather than used in the main arm. It counts "
     "measurement reports by event type in the windows (t − w, t] for w in {1, 2, 5} seconds, the time "
     "since the most recent A3 report, how long the A3 entry condition has held, and the offset, "
     "hysteresis and time-to-trigger in force. These are quantities the network itself holds at t, so "
     "using them is legitimate; they are separated out because a handset-side predictor may not have "
     "them, and because their contribution is worth reporting on its own (Section 5.8)."),
# 4.7 models
377: ("Seven learners are compared under identical folds, identical seeds, the identical hazard "
     "formulation and the identical feature matrix. The last of those is a change from an earlier "
     "draft, in which the sequence models consumed raw per-second measurements while the tabular "
     "models received the engineered features and the history block; under that arrangement the "
     "comparison varied the inputs as well as the learner. Here the sequence models consume windows "
     "of the ten most recent feature vectors — the same vectors, lagged the same way — so the only "
     "thing that varies between arms is the learner."),
383: [("Every arm receives the same tuning budget: twenty random-search trials per outer fold, scored "
     "on a calibration block held out of that fold's training data and separated from it by a "
     "60-second purge. The search is therefore nested inside the evaluation, and the budget is equal "
     "by construction rather than by assertion. Section 5.2 reports the outcome."),
    ("What that nesting does and does not protect against should be stated, because the totals "
     "involved are large: twenty trials across seven learners, four protocols, four outer folds and "
     "three seeds, on a row set carrying roughly six hundred positive rows at the one-second "
     "horizon. What it protects against is selection on the test campaign: no held-out campaign is "
     "ever scored during a search, so the reported metric for any one arm is not an optimised "
     "quantity. What it does not remove is selection across arms. The best of seven learners is "
     "reported beside the other six, and when the best and the second-best differ by less than the "
     "width of their own bootstrap intervals — which Section 5.2 shows is the usual case — the "
     "ranking between them is not a finding, and this thesis does not treat it as one. No "
     "multiplicity correction is applied, because with four independent units none would be "
     "informative; the reader is given the full table and the intervals instead, and every claim in "
     "Chapter 5 that turns on a comparison is stated as a paired difference with an interval rather "
     "than as an ordering of point estimates.")],
# 4.9 protocol
391: ("Motivation. The protocol is the component most likely to invalidate a result on data of this "
     "kind, for two reasons that are usually conflated. Adjacent one-second samples within a session "
     "are near-duplicates, so a random row split places nearly the same row on both sides of it. But "
     "180-second blocks of one continuous session are not independent either: they share cells, "
     "traffic, weather, device state and driver. Section 5.3 measures both."),
392: ("Design. The evaluation protocol has five elements, and a ladder of four splitting rules is "
     "reported rather than one."),
393: ("Splitting ladder. Four splitting rules are compared: leave-one-campaign-out, which holds out a "
     "whole continuous session; blocked cross-validation with a 60-second purge following Roberts et al. "
     f"[{N['ref_roberts']}]; random 180-second blocks of one campaign, which is the protocol this work "
     "used before the audit; and random rows, which is the protocol most of the comparator literature uses. "
     "Section 5.3 reports all four."),
395: ("Repetition and uncertainty. Stochastic learners are run over three seeds and their predictions "
     "averaged. Intervals on pooled out-of-fold metrics come from a bootstrap that resamples whole "
     "180-second blocks within each campaign; that interval is conditional on these four sessions and "
     "does not include between-day variation, so the per-campaign spread is always reported beside "
     "it. Comparisons between learners are reported as per-campaign differences over four paired "
     "observations, with the exact sign-test p-value quoted only to show its own resolution limit "
     "(0.125 at best). No significance claim is made from four units."),
396: DELETE,
397: DELETE,
398: ("Fold hygiene. The feature scaler, the decision threshold, the probability calibrator and the "
     "conformal threshold are fitted inside the training or calibration partition of each fold only, "
     "and the calibration partition is separated from the training partition by the same 60-second "
     "purge."),
399: ("Metrics. Four families are reported. Ranking quality is reported as AUPRC, always with the "
     "prevalence floor and the lift over it, together with AUROC. Calibration is reported as expected "
     "calibration error over fifteen equal-mass bins and Brier score. Coherence is reported as the "
     "fraction of rows on which the horizon ordering is violated, with the magnitude of the "
     "violations. Event-level cost is reported as the fraction of handover commands detected, the "
     "number of false-alarm episodes per hour and per kilometre, and the distribution of lead times. "
     f"Reporting all four families is deliberate: Wagner et al. [{N['ref_wagner']}] show that no "
     "single aggregate metric for event detection satisfies all desirable properties."),
# # 4.10 CRC
401: [("Motivation. A probability is not yet an operating point. An operator needs a decision rule: issue an alarm "
      "when the predicted probability exceeds a threshold. A conventional threshold minimizes a cost or targets a "
      "point on a receiver operating characteristic, but offers no guarantee on future performance. Conformal risk "
      f"control [{N['ref_crc']}], which extends the risk-controlling prediction sets of Bates et al. [{N['ref_bates']}] "
      "from a coverage guarantee to a guarantee on any bounded monotone loss, certifies that the expected risk on "
      "unseen test units does not exceed a chosen level α."),
     ("The exchangeable unit and the loss. The risk is defined per driving unit, not per sample. For "
      "a unit d and decision threshold λ ∈ [0, 1]—where an alarm is issued whenever p̂_t ≥ λ—the row-level "
      "miss loss is the fraction of that unit's positive rows falling below the alarm threshold,"),
     ("R_d(λ) = |{ t ∈ d : y_t = 1 and p̂_t < λ }| / max(|{ t ∈ d : y_t = 1 }|, 1),     (4.9)"),
     ("which is bounded in [0, 1] with B = 1. As the decision threshold λ increases, the alarm rule becomes "
      "stricter, alarms become rarer, and the miss loss R_d(λ) is non-decreasing in λ. Under Theorem 1 of "
      f"Angelopoulos et al. [{N['ref_crc']}], expected risk control E[R_{{n+1}}(λ̂)] ≤ α is achieved by selecting "
      "the most conservative (largest) threshold whose finite-sample empirical bound satisfies the target:"),
     ("λ̂ = sup { λ ∈ [0, 1] : ( n R̂(λ) + B ) / ( n + 1 ) ≤ α },     (4.10)"),
     ("with λ̂ = 0 as the fallback if no threshold satisfies the inequality (which achieves zero miss loss by "
      "alarming continuously, bounded by B = 1). This is a statement about rows, and it is not the "
      "statement Section 1.3 promises. A network operator misses a *handover*, not a row, and a handover "
      "occupies several rows of its lead window. The event-level loss is therefore also carried through: "
      "writing E_d for the handover commands whose lead window lies in unit d, a command at τ is caught "
      "at horizon h if some usable row in [τ − h, τ) alarms, and"),
     ("R_d^ev(λ) = |{ τ ∈ E_d : max_{t ∈ [τ−h, τ)} p̂_t < λ }| / max(|E_d|, 1).     (4.11)"),
     ("A command with no usable row in its window is counted as missed rather than dropped from the "
      "denominator, which is what makes this loss comparable with the detection rate of Appendix A "
      "rather than with the row-level miss rate. Section 5.5 reports both, because they differ and "
      "because the difference is the interesting part."),
     ("R̂(λ) averages the chosen loss over calibration units. The choice of unit is the substantive part of the "
      "claim, and it is tested rather than asserted. Section 5.5 reports the procedure twice: once "
      "with calibration and test units drawn from the same pool of 180-second blocks, where "
      "exchangeability is plausible and the bound should hold; and once with calibration on one whole "
      "campaign and the guarantee claimed for a different campaign, where exchangeability is not "
      "plausible and the bound is not expected to hold. Reporting the second case is the point: a "
      "guarantee whose assumption fails silently is worse than no guarantee."),
     ("α ≥ 1/(n+1)   ⟺   n ≥ 1/α − 1\t(4.12)"),
     ("Equation (4.12) gives the finite-sample feasibility condition for the empirical bound of "
      f"[{N['ref_crc']}]: for the term (n R̂(λ) + B)/(n+1) to reach α when empirical loss is zero (R̂ = 0), "
      "the target must satisfy α ≥ 1/(n+1). This is not an impossibility theorem for risk control itself "
      "(since the trivial fallback λ̂ = 0 always guarantees zero loss by alarming everywhere), but rather the "
      "sample-size floor required for non-trivial threshold selection: a study wishing to promise a non-trivial "
      "5 % miss rate without defaulting to continuous alarm needs at least 19 calibration units, and one promising "
      f"2 % needs 49. With the {N['crc_n_units']} calibration blocks available in a held-out campaign here, the "
      f"tightest non-trivial expressible target is α ≥ {N['crc_floor']:.3f}. The rule converts a modelling question "
      "into a data-collection requirement."),
     ("A second floor binds the event-level loss under the evaluation protocol, arising from preprocessing "
      "and blanking rules rather than from the calibration sample size. A command with no eligible prediction row "
      "in its lead window (due to the 2 s post-handover blanking window and boundary exclusions) cannot be alarmed. "
      f"In the chunk-split calibration folds, {N['crc_unresolvable1']:.1%} of one-second lead windows are in that "
      f"position (against {100 - 100*N['ceiling1']:.1%} in the unbroken sequence, and {N['crc_unresolvable3']:.1%} "
      "at three seconds). No non-trivial event-level target below those values is expressible at any threshold, "
      "so the preprocessing and blanking policy sets the tightest promise a study can certify on handovers.")],
402: DELETE,
403: DELETE,
404: DELETE,
405: DELETE,
406: DELETE,
407: DELETE,
408: DELETE,
409: DELETE,
410: DELETE,

# ---------------------------------------------------------------- CH 5
418: ("This chapter reports what the system of Chapter 4 achieves on the data of Chapter 3. "
     "Section 5.1 reports the alignment audit, because every number after it depends on the audit's "
     "outcome. Section 5.2 reports the primary prediction result and Section 5.3 the protocol ladder. "
     "Sections 5.4 and 5.5 report coherence, calibration and risk control. Sections 5.6 and 5.7 "
     "report generalisation across campaigns and to an independent collection. Section 5.8 reports "
     "what the model uses and where it works, Section 5.9 and 5.10 the two network characterisations, "
     "Section 5.11 the negative results, Section 5.12 the discussion against the objectives and "
     "Section 5.13 the threats to validity."),
419: ("All figures in this chapter are computed out of fold under the leave-one-campaign-out protocol "
     "of Section 4.9 unless stated otherwise, from features lagged as described in Section 4.6."),
# 5.1 (was 5.1 primary result)
420: "5.1 The alignment audit and what it costs",
421: ("The first result of this chapter is not a prediction score. It is a property of the measurement "
     "export that determines what any prediction score on this data means."),
423: ("The audit is simple. For each handover command at time tau, take the export row whose second "
     "contains tau — that is, the row stamped at floor(tau), strictly before the command — and ask "
     "whether its serving-cell identity is already the target of that command. If the row summarises "
     "the second before its timestamp, the answer should be no more often than chance."),
424: ("Table 5.1 and Figure 5.1 report the answer. Across all "
     f"{N['ho_qc']} handovers inside retained blocks the row stamped before the command already "
     f"carries the target cell {N['align_t0']:.1%} of the time, and for handovers isolated from any "
     f"other handover by more than eight seconds the figure is {N['align_iso_t0']:.1%}. One row "
     f"earlier the figure falls to {N['align_iso_tm1']:.1%} for isolated handovers, which is the "
     f"background rate given by rows three seconds earlier ({N['align_iso_tm3']:.1%}). The export row "
     "stamped t therefore describes the state at approximately t + 1: it is a window that closes "
     "after its own timestamp."),
425: ("The consequence for prediction is immediate. A model asked at time t for the probability of a "
     "handover in (t, t + 1] and given the row stamped t is being shown, most of the time, the answer. "
     f"Table 5.2 quantifies it on the same model, the same folds and the same features: the "
     f"one-second AUPRC falls from {N['lag0_auprc1']:.3f} to {N['auprc1']:.3f} and AUROC from "
     f"{N['lag0_auroc1']:.3f} to {N['auroc1']:.3f} when every export feature is lagged one row. At "
     f"five seconds, where one second of misalignment matters less, the same change costs "
     f"{N['lag0_auprc5']:.3f} to {N['auprc5']:.3f}. The single-feature effect is starker: serving "
     f"dwell time computed from the export's serving-cell column reaches AUROC "
     f"{N['lag0_dwell_auroc']:.3f} on its own, and the same quantity computed from signalling "
     f"timestamps reaches {N['dwell_auroc']:.3f}."),
426: ("Three points are worth stating plainly. First, this is not a defect of the instrument in the "
     "sense of a fault: a one-hertz export has to summarise a second, and a row that carries the "
     "state at the end of its second is a defensible choice. It is a defect of any analysis that "
     "assumes the opposite without checking. Second, the error is invisible to every check that does "
     "not involve the event clock — the features are causal in appearance, no future label is used, "
     "and grouped splitting does not remove it, because the leak is inside the row rather than "
     "between rows. In the taxonomy of Kaufman et al. "
     f"[{N['ref_kaufman']}] it is leakage in training examples rather than leakage in feature "
     "construction: no illegitimate feature was built, and no future row was consulted; the "
     "legitimate feature simply describes an interval that already contains the event. That class is "
     "the one their paper identifies as hardest to catch, because every syntactic check passes. "
     "Third, it is cheap to detect, and the audit above is the recommendation this thesis makes to "
     "anyone building a predictor on a drive-test export."),
427: [("Why the row behaves this way is not a mystery, and stating the reason turns the observation "
      "into a prediction that can be falsified. A handover command is issued at an arbitrary instant "
      "tau inside the second [t, t+1). Write delta = tau - floor(tau) for where inside the second the "
      "command falls, x for the time the handset takes to be served by the target and for that change "
      "to reach the serving-cell register, and"),
      ("m = 1 - delta - x,   with delta, x and m all in seconds     (5.1)"),
      ("for the margin by which that change beats the close of the second. If the exporter writes one "
      "row per second carrying the state as of the end of the second, the row stamped t shows the "
      "target cell when m > 0 and the source cell when m < 0. Nothing about the network enters: the "
      "defect is what end-of-second aggregation does to an event whose execution is two orders of "
      "magnitude faster than the grid."),
      ("The prediction is testable on our own captures, because the signalling stream carries the "
      "RRCConnectionReconfigurationComplete message and therefore x for each handover "
      f"({N['mech_exec_nonzero']:.0%} of commands record a non-zero interruption; the ninetieth "
      f"percentile is {N['mech_exec_p90_ms']:.0f} ms and the largest is {N['mech_exec_max_ms']:.0f} "
      f"ms). Of the {N['mech_n_neg']} handovers whose execution crosses the second boundary, "
      "m < 0, not one has the target cell on the row stamped t. Of the "
      f"{N['mech_n_pos']} with m > 0, the row shows the target cell "
      f"{N['mech_hit_pos']:.1%} of the time. The same gradient appears in delta directly: on "
      f"handovers isolated from any other by two seconds, contamination runs at "
      f"{N['mech_delta_early']:.1%} for commands in the first quarter of the second and falls to "
      f"{N['mech_delta_late']:.1%} for commands in the last quarter, where the margin is smallest. "
      "Panels (d) and (e) of Figure 5.1 show both."),
      (f"The residual matters for honesty about the size of the effect. In {N['mech_resid_share']:.1%} "
      "of handovers the margin is positive and the row stamped t nevertheless shows the source cell; "
      f"in {N['mech_resid_tp1']:.0%} of those the row stamped t+1 carries the target instead. That is "
      "the exporter refreshing the serving-cell field one row late, not a second mechanism. That "
      "sentence was an assertion in an earlier draft and is now evidence: Table A.6 resolves the "
      f"residual one handover at a time. Of the {N['resid_n']} handovers whose margin is positive "
      f"and whose row t nevertheless shows the source, {N['resid_tp1_n']} \u2014 "
      f"{N['resid_tp1_share']:.0%} \u2014 have the target cell in row t+1, arriving exactly one row "
      "late, and the table lists ten of them with the cell identity in each row so the pattern can "
      "be read rather than taken on trust. The competing explanation, that the residual is simply "
      "the small-margin cases where the geometry was marginal, does not survive the same table: the "
      f"median margin of the residual is {N['resid_median_margin']:.3f} s against "
      f"{N['resid_median_margin_hit']:.3f} s for the contaminated handovers, which is no separation "
      "at all. Our export therefore shows an attenuated form of the defect, and the uncontaminated "
      f"figure for it is not zero but about {1 - N['align_t0']:.0%}."),
      ("Two independent datasets test the account. The first is the control. The 4G/5G Irish "
      f"drive-test traces of Raca et al. [{N['ref_raca']}] are {N['ext_traces']} driving traces logged "
      "at one hertz with a different tool, G-NetTrack Pro, on a different operator. That tool samples "
      "the modem rather than aggregating a second, so the account predicts no contamination at the "
      "level an aggregate produces, and there is none at that level: "
      f"{N['ext_align_share']:.1%} of {N['ext_align_n']:,} cell changes have row t "
      f"looking like the new cell, against {N['align_t0']:.1%} of handovers in our export and "
      f"{N['nw_t0']:.1%} under an end-of-second rule. That figure is not zero, and it is not claimed "
      "to be: with a ping-pong rate above a quarter the cell serving three rows earlier is often the "
      "cell about to serve again, so a sampled log has an elevated background for reasons unrelated "
      "to aggregation. The discriminating quantity is the shape rather than the level. The "
      "prediction test agrees. Using the row stamped t rather than the row before it is worth "
      f"+{N['ext_gain_t']:.0f} % AUPRC on that dataset, but the step from t - 2 to t - 1 is worth "
      f"+{N['ext_gain_t1']:.0f} %, a larger gain. A contaminated final row shows up as an anomalous "
      "jump at the last step; a clean log shows the smooth decay with staleness that this dataset "
      "has."),
      ("The second dataset does not replicate the defect on a third instrument; it does something "
      "narrower and more useful, by letting the aggregation rule be set rather than inferred. "
      "The NUWiNS multi-carrier corpus "
      f"[{N['ref_nuwins']}], collected with XCAL on two cross-United-States routes over three tier-one "
      f"operators and released as raw exports at ten hertz [{N['ref_wheels']}], carries the handover "
      "event clock and the serving-cell register at native rate. Downsampling it to one hertz by "
      f"keeping the last sample of each second reproduces the defect: over {N['nw_n']:,} "
      f"cell-changing handovers in {N['nw_files']} files and {N['nw_operators']} operators, the row "
      f"stamped t carries the target cell {N['nw_t0']:.1%} of the time "
      f"({N['nw_iso_t0']:.1%} on the {N['nw_iso_n']:,} isolated handovers), and the row stamped t - 1 "
      f"carries it {N['nw_tm1']:.1%} of the time. Keeping the first sample of each second instead "
      f"collapses contamination to {N['nw_first_t0']:.1%} on the same handovers. One rule produces "
      "the defect and the other removes it, on data the network never saw differently. What this "
      "establishes is that end-of-second aggregation is sufficient to produce the artefact at the "
      "magnitude observed, and how large it is when present. It does not establish that XCAL's own "
      "one-hertz exporter aggregates that way, which remains inferred from its consequences and "
      "which only a native-rate re-export of these captures would settle; Section 5.13 carries that "
      "as a residual risk."),
      ("The tail of the second then measures something the one-hertz grid was never supposed to "
      "resolve. Within a bin of delta, the row stamped t is contaminated precisely when the "
      "serving-cell register updates before the second closes, so the contamination share in that bin "
      "estimates the probability that the update delay is shorter than the time remaining. Over "
      f"{N['nw_cliff_n']:,} handovers arriving in the last fifth of a second, contamination is "
      f"{N['nw_cliff_hi']:.1%} when 75 to 100 ms remain, {N['nw_cliff_mid']:.1%} when 50 to 75 ms "
      f"remain and {N['nw_cliff_lo']:.0%} when less than 50 ms remain. The cliff is the distribution "
      "of the update delay, and it sits at roughly 60 ms — consistent with the native ten-hertz view "
      f"of the same events, where the target is already serving at tau + 100 ms in "
      f"{N['nw_native_p100']:.0%} of handovers. Panel (f) of Figure 5.1 shows the curve. The "
      "alignment defect is therefore not only a hazard to be removed; read deliberately, it is a "
      "sub-second measurement recovered from a one-hertz export."),
      ("Three conclusions follow. The alignment error is a property of the aggregation rule, not of "
      "the vendor and not of the network: the same tool produces it on three operators and two "
      "continents, and a tool that samples instead of aggregating does not. It is predictable, which "
      "means it can be audited in a few lines against any event clock, and the audit reports a "
      "mechanism rather than an anomaly. And it is consequential, because none of the 22 models "
      "audited in Section 2.3 reports such a check, and neither did the first version of this work."),
      ("One objection has to be answered before the repair can be trusted, because it would change "
      "what the audit means. Lagging every export column removes the contaminated serving-cell "
      "assignment, but it also discards one second of radio and kinematic history that the "
      "aggregation rule cannot plausibly corrupt. On that reading the fall from "
      f"{N['sel_v1_auprc1']:.3f} to {N['sel_v2_auprc1']:.3f} would measure the cost of a blunt "
      "repair rather than the size of the leak, and the honest correction would be a targeted one. "
      "Table 5.2b settles it. The four arms share one row set, one set of folds, one learner and one "
      "set of seeds; only the set of columns shifted by one row changes."),
      ("The leak is carried almost entirely by the serving-cell radio scalars. Lagging the "
      f"{N['sel_nbr_cols']} neighbour and gap columns alone — the A3 condition itself, and the "
      "columns any analyst would suspect first — leaves the one-second AUPRC at "
      f"{N['sel_nbr_auprc1']:.3f}, which is {N['sel_share_nbr']:.1%} of the total fall. Lagging the "
      f"{N['sel_srv_cols']} serving-cell radio columns alone takes it to "
      f"{N['sel_srv_auprc1']:.3f}, which is {N['sel_share_srv']:.1%} of it. And lagging the mobility "
      "block as well — the GPS speed, acceleration and bearing that no aggregation rule can "
      f"contaminate — is worth {abs(N['sel_share_gps']):.1%} in the opposite direction, which is to "
      "say nothing at all. The repair is therefore not conservative: the blunt version and the "
      "targeted version cost the same, so the figure quoted throughout this thesis measures the leak "
      "and not the remedy."),
      ("Why the radio scalars and not the cell identity is worth stating, because it is the part "
      "that transfers. A column named serving_rsrp is not a property of a cell; it is the reference "
      "signal power of whichever cell the export currently treats as serving. When the assignment "
      "flips inside the second, that column silently changes its referent and reports the target's "
      "power, which is by construction higher than the source's — an ordinary-looking radio scalar "
      "that is in fact the answer. The same is true of the RSRQ, SINR and CQI families and of every "
      "moving average and slope derived from them. The neighbour gap moves too, but it was already "
      "near its trigger threshold, so the flip adds little to it. The practical recommendation is "
      "therefore narrower and more useful than lag everything: on any export whose rows are "
      "aggregates, the columns to distrust are the ones defined relative to the serving-cell "
      "assignment, and they are not the ones that look like identifiers."),
      ("Everything reported after this section uses the lagged features. The unlagged numbers are "
      "retained in Table 5.2 and in the ablation of Section 5.11 as a measurement of the effect, "
      "never as a result. Table 5.2b is reported under fixed hyper-parameters so that the five arms "
      "are comparable with one another; its levels are not comparable with Table 5.3, which reports "
      "the tuned arm.")],
# 5.2 primary result
430: "5.2 The primary prediction result",
431: ("Table 5.3 reports the performance of the primary model, LightGBM under the hazard formulation, "
     "at all five horizons, out of fold under leave-one-campaign-out."),
434: ("One second before a handover command the model reaches AUPRC "
     f"{N['auprc1']:.3f} against a prevalence floor of {N['prev1']:.1%}, a lift of "
     f"{N['lift1']:.1f}x, at AUROC {N['auroc1']:.3f} and an expected calibration error of "
     f"{N['ece1']:.3f}. At five seconds it reaches AUPRC {N['auprc5']:.3f} against a "
     f"{N['prev5']:.1%} floor, a lift of {N['lift5']:.1f}x. An A3-derived baseline—defined by evaluating the "
     "continuous A3 signal margin (neighbour RSRP minus serving RSRP, offset, and hysteresis) at the 1 Hz sample "
     f"instants—scores AUPRC {N['a3_auprc1']:.3f} at one second ({N['a3_lift1']:.1f}x) and AUROC {N['a3_auroc1']:.3f}. "
     "Because 1 Hz sampled exports cannot reconstruct sub-second continuous time-to-trigger timers (160–640 ms), "
     "this baseline evaluates a signal-margin proxy rather than the eNodeB's internal sub-second state machine. On these "
     "evaluated rows, the learned multi-horizon hazard model outperforms this specified A3-derived baseline, while scoring "
     "materially below its pre-audit unlagged level; both observations belong in the same paragraph."),
435: ("Two features of Table 5.3 require comment. First, the lift falls with the horizon while the "
     "AUPRC rises, because the prevalence floor rises faster than the score does; reading AUPRC "
     "across rows without the floor is precisely the error Section 2.3 documents in the comparator "
     "literature. Second, the calibration error rises with the horizon, so the five-second output "
     "should be read as a ranking rather than as a probability."),
436: [("The interval on each AUPRC in Table 5.3 comes from a block bootstrap over 180-second blocks "
     "and is conditional on these four sessions. It measures variation within the corridors driven, "
     "not variation between corridors, and Section 5.3 shows those blocks are not independent of one "
     "another, so the interval is narrower than the evidence about a fifth campaign would support. "
     "The per-campaign spread in Table 5.8 is wider, and it is the honest measure of how much a "
     "fifth campaign could move these numbers. No interval anywhere in this thesis is used to "
     "declare a difference significant."),
    ("That applies directly to Table 5.4, which is a leaderboard and should not be read as one. "
     f"{N['rank1_learner']} is first at {N['auprc1']:.3f}, {N['rank2_learner']} second at "
     f"{N['rank2_auprc']:.3f} and {N['rank3_learner']} third at {N['rank3_auprc']:.3f}, and the "
     f"intervals of {N['rank_overlap_n']} of the arms overlap the leader's. The ordering of the top "
     "arms is not a finding of this thesis, and none of its conclusions depends on it. What the "
     "table is for is the comparison of levels against the prevalence floor and against the "
     "deployed rule, which is a gap of a different order from the gaps between learners. The same "
     "caution applies to the tuning: twenty trials over seven learners, four folds and three seeds "
     "is a large search relative to roughly six hundred positive rows at one second, and the "
     "protocol of Section 4.7 protects the held-out campaign from it without removing the "
     "researcher's freedom to have chosen the search in the first place. The alignment audit and "
     "the feature lag were themselves decided after all four campaigns were in hand; Section 3.2 "
     "records the freeze, and the honest description of everything after it is exploratory "
     "development rather than confirmatory evaluation."),],
# 5.3 protocol ladder
438: "5.3 Splitting protocol: what each unit is worth",
439: ("The result in this section concerns the protocol rather than the predictor, and it is the one "
     "with the widest implication for the comparator literature."),
440: ("The experiment is a controlled substitution. Everything is held fixed — the data, the lagged "
     "features, the formulation, the hyper-parameters, the seeds — and only the splitting rule "
     "changes, across the four-step ladder of Section 4.9. Table 5.5 reports the result at one second "
     "and Figure 5.5 across the ladder."),
442: ("Three readings follow. Random-row splitting inflates the one-second AUPRC of the primary model "
     f"by {N['infl_row_lgbm']:.0f} % over leave-one-campaign-out, and of the recurrent arm by "
     f"{N['infl_row_gru']:.0f} %. Splitting by random 180-second blocks — grouped splitting, and the "
     f"protocol this work used before the audit — still inflates it by "
     f"{N['infl_chunk_lgbm']:.0f} %, because blocks of one continuous session are not independent of "
     "one another. Blocking contiguously in time with a 60-second purge removes most of that: it sits "
     f"within {N['infl_block_lgbm']:.0f} % of the leave-one-campaign-out figure, which is what makes "
     "it a usable protocol when only one session is available."),
443: ("The mechanism is straightforward once stated. Two samples one second apart on the same drive "
     "are near-duplicates. Two 180-second blocks of the same session are not duplicates, but they "
     "share cells, traffic and trajectory, so a model can recognise the session rather than the "
     "situation. The implemented A3 signal-margin proxy, which has no learned parameters to over-fit with, moves by "
     f"{N['infl_row_a3']:+.0f} % across the whole ladder and is the control that shows the effect is "
     "over-fitting rather than a difference in test-set difficulty."),
444: ("Because the inflation is architecture-dependent, it reorders the ranking rather than raising it "
     f"uniformly. Under leave-one-campaign-out the leading learner at one second is "
     f"{N['top_loco']}; under random-row splitting it is {N['top_random']}, whose inflation is "
     f"{N['infl_row_top_random']:.0f} % against {N['infl_row_top_loco']:.0f} % for the former. A study "
     "using the standard protocol of the comparator literature would therefore report a different "
     "winner on the same data. This is the answer to objective O2, and it is why the protocol is "
     "presented as a contribution rather than as housekeeping."),
# 5.4 coherence
447: ("Table 5.6 reports the coherence and calibration behaviour of the hazard formulation against the "
     "independent-classifier control and the three post-hoc repairs of Section 4.2."),
449: ("The hazard formulation produces no horizon-ordering violations, which follows from "
     "Equation (4.5). The independent-classifier arm violates ordering on "
     f"{N['viol_indep']:.1%} of rows, with a mean violation of {N['viol_indep_mean']:.3f} and a "
     f"maximum of {N['viol_indep_max']:.3f} where it occurs. Every violation figure in this "
     "thesis is the rate of a single fitted system, which is what a deployment would meet; "
     "Table 5.6 reports beside it the rate of the three-seed ensemble, which is lower "
     f"({N['viol_indep_ens']:.1%} for the same arm) because averaging predictions smooths "
     "violations away rather than removing their cause."),
450: [("The honest comparison, however, is not against the raw control. A per-row cumulative maximum "
     "removes every violation at no cost, needs no held-out split, and is two lines of code; the "
     "pool-adjacent-violators projection does the same. Both are reported in Table 5.6, and an "
     "earlier draft of this work omitted them, which overstated the case for the formulation. "
     "Isotonic calibration per horizon is the most expressive control: on its own it makes coherence "
     f"worse (violations rise from {N['viol_indep']:.1%} to {N['viol_iso']:.1%}, because the K "
     "isotonic maps are fitted independently and may reorder the horizons), but composed with the "
     f"monotone projection it reaches zero violations and the lowest calibration error of any arm "
     f"({N['ece1_isopav']:.3f} at one second against {N['ece1_hazard']:.3f} for the hazard model). "
     "That composite is therefore the arm to beat, and the hazard formulation does not beat it on "
     "calibration."),
      ("What the hazard formulation retains after that correction is narrower and worth stating "
     "exactly. It is the only arm that reaches coherence and competitive calibration from a single "
     f"fit with no held-out calibration split: {N['ece1_hazard']:.3f} at one second against "
     f"{N['ece1_indep']:.3f} for raw independent heads and {N['ece1_cummax']:.3f} for the "
     "cumulative-maximum control, none of which consume data, while the isotonic arms spend a "
     "calibration block that a four-session dataset can ill afford, and it estimates one model "
     "rather than five."),
      ("How those differences are tested matters more than their size, and an earlier version of "
     "this section got it wrong. That version bootstrapped the paired differences over 180-second "
     "blocks and reported which intervals excluded zero. Section 5.3 of this same thesis proves "
     "those blocks are not independent — splitting on them inflates one-second AUPRC by "
     f"{N['infl_chunk_lgbm']:.0f} % — so resampling them deflates the standard error and "
     "manufactures a confidence interval narrower than the evidence supports. The claim is "
     "withdrawn. The block interval is retained in Table 5.6b as a descriptive within-campaign "
     "spread and carries no inference. What carries the conclusion is the column beside it: the "
     f"paired difference computed separately on each of the {N['n_campaigns']} held-out campaigns, "
     "which are the only units in this dataset whose independence is defended. With four units a "
     f"two-sided sign test cannot return a p-value below {N['min_p']:.3f}, so no test is run and "
     "none is needed; the four numbers are printed and the reader may judge them."),
      ("Read that way, the calibration result holds and the ranking result does not. The hazard arm "
     f"has the lower calibration error on {N['ece_wins']} of the {N['n_campaigns']} held-out "
     f"campaigns, by {abs(min(N['dif_ece_indep_percap'], key=abs)):.3f} to "
     f"{abs(max(N['dif_ece_indep_percap'], key=abs)):.3f} against raw independent heads, and the "
     "same holds against both no-split projections. That is as strong a statement as four units "
     "permit, and it is consistent rather than significant. The isotonic composite is the "
     f"exception in the other direction: it calibrates better than the hazard arm on "
     f"{N['n_campaigns'] - N['ece_wins_iso']} of the {N['n_campaigns']} campaigns but worse on the "
     "highway campaign, so its advantage is real on urban corridors and does not survive the one "
     "regime change this dataset contains — a caveat an earlier draft missed by reporting only the "
     "pooled number. Ranking is a different story: the one-second AUPRC difference between the "
     f"hazard arm ({N['auprc1_hazard']:.3f}) and the raw independent control "
     f"({N['auprc1_indep']:.3f}) is {N['dif_auprc_indep']:+.3f} pooled but positive on all four "
     "campaigns taken separately. A difference that reverses sign between the pooled and the "
     "per-campaign view, at a magnitude of a thousandth, is not a result in either direction, and "
     "no ranking claim is made from it."),
      ("Two things the formulation does not retain should be said in the same breath. It does not "
     "rank better, for the reason just given. And the censoring-aware "
     "likelihood of Section 4.4, sometimes cited as an advantage of the survival formulation, is "
     "inert here, because the row filter of Section 3.7 leaves no row censored inside the horizon "
     "grid; it is retained for generality, not counted as a benefit. On a larger dataset, isotonic "
     "calibration followed by a monotone projection would be the stronger choice, and this thesis "
     "says so.")],
451: DELETE,
452: DELETE,
# 5.5 CRC
458: ("Table 5.7 reports conformal risk control in the two regimes described in Section 4.10."),
460: ("Four observations follow."),
461: ("First, in the exchangeable regime — calibration (n ≈ 43) and test units (n ≈ 14) drawn "
     "from the 57 pooled 180-second blocks across the corpus (populated with out-of-fold predictions "
     "from the four leave-one-campaign-out fits, held strictly separate from model fitting and tuning) — "
     "the realised mean per-unit miss rate sits at or below the target at every level tested. "
     f"At α = 0.20 the certified threshold alarms on {N['crc_alarm20']:.0%} of rows and realises a mean "
     f"per-unit miss rate of {N['crc_loss20']:.3f}. Note that while random block pooling breaks macro-campaign "
     "boundaries, it does not guarantee exchangeability of losses due to remaining spatio-temporal autocorrelation; "
     "it serves as an empirical benchmark."),
462: ("Second, in the regime an operator faces when deploying across sessions—calibrating on one "
     "campaign and deploying on another—exchangeability is violated by corridor shifts and differing speed "
     f"profiles. Importantly, Theorem 1 of Angelopoulos et al. [{N['ref_crc']}] bounds the expected risk "
     "E[R_{n+1}(\\hat{\\lambda})] ≤ α under the assumption of exchangeable calibration and test units. "
     f"Across the {N['crc_rotations']} cross-campaign rotations at α = 0.20, the empirical mean miss loss "
     f"was {N['crc_cross_loss20']:.3f} at a target of 0.20. Cross-campaign results are descriptive because "
     "the required exchangeability assumption is not established across distinct physical environments "
     f"(with individual test rotations realizing losses spanning around the target, and empirical holdout "
     f"loss falling below α in {N['crc_cross_ok20']:.0%}, or 10 of 12, rotations). Between-campaign differences "
     "in velocity and base-station topology represent distribution shifts that violate formal exchangeability, "
     f"so cross-session guarantees remain exploratory without explicit domain adaptation [{N['ref_qchohi']}]; "
     "Section 7.3 records it as the next step rather than as a gap this thesis fills."),
463: [("Third, the guarantee is expensive at tight targets, and the number of calibration units binds "
       f"independently of model quality. At α = 0.05 the threshold alarms on {N['crc_alarm05']:.0%} of "
       "rows in the exchangeable regime, which is not an operating point any operator would deploy. "
       "Across the four campaigns, block counts are n ∈ {8, 14, 15, 20} (mean n = 14.25). For the empirical "
       "bound to be feasible with zero empirical loss requires α ≥ 1/(n+1). The four individual floors are "
       "1/9 = 0.111 (Campaign 2), 1/15 = 0.067 (Campaign 4), 1/16 = 0.063 (Campaign 1), and 1/21 = 0.048 "
       "(Campaign 3), giving an arithmetic mean feasibility floor of 0.072 across campaigns. Only the "
       "largest campaign (n = 20, 25% of rotations) satisfies 1/(n+1) ≤ 0.05, permitting non-trivial threshold "
       "calibration at \\hat{\\lambda} = 0.0 (alarming on all eligible rows); the other 75% default to constant-alarm fallback."),
      ("Fourth, and most consequentially for what may be promised, the estimand matters. Everything "
      "above certifies the row-level loss of Equation (4.9). What an operator would understand by a miss "
      "is the event-level loss of Equation (4.11): a handover is caught if any eligible row in its lead "
      "window alarms. The two are not interchangeable, because a command with no eligible prediction row "
      "in that window (due to the 2 s post-handover blanking window and boundary exclusions) is unresolvable "
      f"at every threshold. In the chunk-split calibration folds, {N['crc_unresolvable1']:.1%} of commands "
      f"are in that position at one second (compared to {100 - 100*N['ceiling1']:.1%} in the unbroken sequence), "
      "so no event-level target below that value is expressible without defaulting to continuous alarm, and "
      f"none of the five targets in Table 5.7 is feasible in more than {N['crc_ev_feasible1_max']:.0%} of the "
      f"rotations. At three seconds, where only {N['crc_unresolvable3']:.1%} of commands are unresolvable, "
      f"the picture changes: α = 0.20 is feasible in {N['crc_ev_feasible3_20']:.0%} of rotations at a realised "
      f"event loss of {N['crc_ev_loss3_20']:.3f} and an alarm rate of {N['crc_ev_alarm3_20']:.0%}, and α = 0.30 "
      f"in all of them at {N['crc_ev_loss3_30']:.3f} and {N['crc_ev_alarm3_30']:.0%}."),
      ("The conclusion is worth stating plainly, because it is a limit rather than a result. Under the "
      "preprocessing and blanking policy adopted here, the exclusion of burst-interior and boundary rows sets "
      "the tightest promise that can be made about handovers: at one second a distribution-free bound on the "
      "fraction of handover commands missed cannot be certified below 36.4% without continuous alarm. At three "
      "seconds the promise becomes expressible, and costs an alarm on roughly half of all seconds. This reconciles "
      "Table 5.7 with the detection rates of Appendix A, which are event-level throughout and which no row-level "
      "guarantee bounds."),
      ("Figure 5.9 puts both regimes on one pair of axes, with the realised risk against the nominal "
      "level and the guarantee boundary drawn as the diagonal; the second axis carries the alarm rate "
      "that compliance costs.")],
464: DELETE,
# 5.6 LOCO
467: ("Objective O4 asks whether the result survives outside the data on which it was developed. Two "
     "experiments address it, at increasing distance from the training distribution."),
468: ("The first is the primary protocol itself: each campaign held out in turn. Table 5.8 reports the "
     "per-campaign numbers that the pooled result of Table 5.3 averages over."),
470: ("The spread across campaigns is the useful quantity. At one second the AUPRC lift runs from "
     f"{N['loco_lift_min']:.1f}x to {N['loco_lift_max']:.1f}x across the four held-out campaigns, and "
     f"AUROC from {N['loco_auroc_min']:.3f} to {N['loco_auroc_max']:.3f}. The weakest campaign is "
     f"{N['loco_worst']}, and the highway campaign — the one that differs in both corridor and speed "
     f"regime — reaches AUROC {N['loco_auroc_highway']:.3f}. No campaign carries the pooled result, "
     "and none of them is close to the figure this work reported before the alignment audit."),
471: ("The honest reading of the spread is that a fifth campaign could plausibly land anywhere inside "
     "it, and that four units are too few to say more than that. That is a statement about the size "
     "of the study, not about the method."),
# 5.7 external
472: "5.7 An independent collection on the same network",
473: ("The second generalisation experiment uses a publicly released LTE drive-test dataset "
     f"[{N['ref_shafi_ds']}]. Its provenance is stated exactly, because an earlier draft of this work "
     "described it as a different network: it was collected by a different group of people on a "
     "different route two years earlier, using the same operator, the same city and the same XCAL "
     "licence, in the same department of the same institution. It is an independent collection on the "
     "same network, and it tests collection, route and time — not operator, and not radio access "
     "technology."),
474: ("Because the public dataset publishes measurement-report exports rather than a periodic radio "
     "export, both domains are rebuilt here from the L3 signalling alone, on the same feature "
     "construction, so that the comparison varies the dataset and nothing else. That construction "
     "also sidesteps the alignment problem of Section 5.1 by using millisecond report timestamps "
     "directly. Table 5.9 reports the four directions with bootstrap intervals over runs."),
475: [("The transferred model reaches AUROC "
     f"{N['ext_ours_to_pub_auroc']:.3f} on the public data without refitting, against "
     f"{N['ext_pub_auroc']:.3f} for a model trained on that dataset itself and "
     f"{N['ext_ours_auroc']:.3f} for our own leave-one-capture-out result on the same "
     "signalling-only feature set. The intervals overlap. The reading is that the formulation and the "
     "feature construction transfer to another collection on this network, which is a weaker and more "
     "defensible claim than the one made in the earlier draft."),
    ("One limitation of this experiment has to be stated at full strength rather than left for a "
     "reader to infer, because it bounds every generalisation claim in this thesis. Both arms of "
     "Table 5.9 are signalling-only. The public dataset does not publish a periodic radio export, so "
     f"the radio and mobility blocks — {N['n_feat_radio']} and {N['n_feat_mobility']} of the "
     f"{N['n_features']} features the primary model uses, and the source of essentially all of its "
     "signal — cannot be constructed on it at all. What Table 5.9 therefore tests is the transfer "
     f"of a {N['n_feat_history']}-feature signalling model, not of the system reported in "
     "Section 5.2. "
     "The primary model has never been evaluated outside the four campaigns it was built on, and no "
     "experiment in this thesis establishes that it would transfer. That is a limitation of the "
     "available public data rather than a choice, and it is the single strongest argument for the "
     "second-operator collection proposed in Section 7.3; until such data exist, the external "
     "evidence in this thesis covers the formulation and the feature construction, not the "
     "predictor.")],
# 5.8 mechanism
477: "5.8 What the model uses, and where it works",
478: ("Objective O5 asks for the mechanism rather than the score. With the alignment corrected, the "
     "answer is less dramatic than the earlier draft claimed and more useful."),
479: ("Table 5.10 reports the strongest single features, each evaluated alone at the one-second "
     "horizon on the lagged export."),
482: ("Four results follow. No single feature is close to the model: the strongest reaches AUROC "
     f"{N['best_single_auroc']:.3f} against {N['auroc1']:.3f} for the full model, so the signal is "
     "distributed rather than concentrated in one clock. The strongest single quantity is the time "
     f"since the last Event A3 report ({N['best_single']}, AUROC {N['best_single_auroc']:.3f}), which "
     "is a signalling quantity the network holds in real time and which the main feature set "
     f"deliberately excludes; the strongest export feature is serving SINR ({N['best_export_auroc']:.3f}). "
     f"Serving dwell time computed from signalling timestamps reaches {N['dwell_auroc']:.3f}, ahead "
     "of chance and far behind its unlagged counterpart. And the serving-to-neighbour gap on which "
     f"Event A3 is defined reaches {N['gap_auroc']:.3f}, which is the one part of the earlier "
     "mechanism claim that survives: the quantity the rule thresholds on is a weak predictor of "
     "imminence, because it is a permission condition and not a tendency."),
483: [("Where the model works matters as much as what it uses. Handover commands arrive in bursts — the "
     f"median gap between consecutive commands is {N['median_gap_s']:.1f} seconds — and a predictor "
     "can score well by recognising that it is inside a burst. Table 5.11 therefore splits the "
     "out-of-fold rows into those with no handover in the previous ten seconds and those inside a "
     f"burst. At one second the lift is {N['burst_quiet_lift1']:.1f}x on quiet rows and "
     f"{N['burst_inburst_lift1']:.1f}x on in-burst rows, against prevalence floors of "
     f"{N['burst_quiet_prev1']:.1%} and {N['burst_inburst_prev1']:.1%}. A history-only model reaches "
     f"a lift of {N['hist_lift1']:.1f}x pooled, so burst membership alone does not account for the "
     "result, but the radio block earns most of its advantage on the quiet rows, which is where an "
     "operator would want the warning."),
    ("That comparison needs two qualifications, and an earlier draft made neither. The first is "
     "that \u201cquiet\u201d is not a synonym for \u201cisolated\u201d. Every burst begins on a "
     "row that is itself quiet, so a quiet positive row is by construction the row before a burst "
     "starts rather than the row before a lone handover. Measured directly, "
     f"{N['burst_init_quiet_n']} of the {N['burst_init_quiet_n']} positive quiet rows at one second "
     f"\u2014 {N['burst_init_quiet_share']:.0%} of them \u2014 precede a command that opens a new "
     f"burst, against {N['burst_init_inburst_share']:.1%} of the positive in-burst rows. The claim "
     "this section supports is therefore that the model predicts the onset of a burst better than "
     "it predicts continuation inside one, which is a sharper statement than the one about quiet "
     "rows and is also the more useful one: the onset is the event an operator cannot already see "
     "coming from the command stream."),
    ("The second qualification is that the two strata do not share a prevalence floor, so their "
     "AUPRC values are not directly comparable and only the lift is. Because the cutoff of ten "
     "seconds is itself a choice made after the data were in hand, Table A.5 varies it from five to "
     f"twenty seconds. The ordering holds at every value tested: the quiet lift runs from "
     f"{N['sens_burst_quiet_min']:.1f}x to {N['sens_burst_quiet_max']:.1f}x and the in-burst lift "
     f"from {N['sens_burst_burst_min']:.1f}x to {N['sens_burst_burst_max']:.1f}x, and the quiet "
     f"stratum is ahead in all {N['sens_burst_n']} of them. The conclusion does not depend on where "
     "the line was drawn, which is the only thing a post-hoc cutoff can be asked to demonstrate.")],
484: ("Figure 5.12 ranks the single features and Figure 5.13 gives the dose-response relation between "
     "the time since the previous command and the probability of the next one, which is monotone up "
     "to about twenty seconds and flat thereafter."),
485: ("Two further sensitivities are reported here rather than buried. Censoring the "
     f"{N['competing_rows']} model rows whose next event is a re-establishment rather than a "
     "handover command — a count of rows in the horizon grid, not of the "
     f"{N['reest_all']} re-establishment events themselves — moves the one-second lift by "
     f"{N['censor_delta_lift1']:+.2f}x, so the competing risk of "
     "Section 3.6 does not drive the result. And adding the signalling block of Section 4.6 to the "
     f"radio, mobility and history blocks moves the one-second AUPRC from {N['auprc1']:.3f} to "
     f"{N['abl_sig_auprc1']:.3f}; the signalling block on its own reaches "
     f"{N['abl_sigonly_auprc1']:.3f}."),
# 5.9 conversion
488: ("The first of the two network characterisations concerns what happens to a measurement report "
     "after it is sent, and it is reported here in a corrected form. Event A3 report configurations "
     "on this network carry reportInterval and reportAmount values that cause a triggered condition "
     "to be re-reported periodically until it ceases: the modal configuration re-reports every 240 ms "
     "with reportAmount infinity. Counting transmitted reports therefore counts re-reports of the "
     "same trigger, and an earlier draft of this work did exactly that."),
487: "5.9 The A3 report-conversion rate and the unit it is counted in",
491: [("Table 5.12 reports the conversion rate under three units, because the quantity has no "
     "unit-free value. Grouping consecutive reports of one measId whose spacing is consistent with "
     f"the configured reportInterval into a single trigger episode reduces {N['a3_reports']:,} "
     f"transmitted A3 reports to {N['a3_episodes']:,} episodes, an average of "
     f"{N['reports_per_episode']:.1f} reports per episode. Counted per report, "
     f"{N['conv_rep_declined']:.0%} of A3 reports are not followed by a handover command within two "
     "seconds. Counted per episode, asking only whether some command falls in the two seconds after "
     f"the episode ends, the figure is {N['conv_ep_declined']:.0%} not followed by a command."),
      ("That second figure is not a true conversion rate, and an earlier version of this work reported it "
     "as though it were. Several measIds monitor different neighbours and fire within the same "
     f"approach to a cell boundary, so the {N['a3_episodes']:,} episodes claim "
     f"{N['conv_mult_any']:,} commands between them out of the {N['ho_all']} that exist: the same "
     f"command is claimed by {N['conv_mult_factor']:.1f} episodes on average. Matching each command "
     "to exactly one episode containing its most proximate associated report—and resolving the 16 multi-command "
     f"episodes where rapid consecutive handovers occur—leaves {N['conv_mult_own']} episodes owning commands, so "
     f"{N['conv_ep1_declined']:.0%} of episodes are not followed by a matched command. Under loose attribution, a "
     "configuration that re-reports on several identities scores a lower unconverted rate precisely "
     "because it is verbose, which inverts the interpretation the rate is used for."),
      ("The sensitivity to definition is the central finding. A rate quoted without its unit is a statement "
     "about counting conventions rather than base-station control policy, and the unconverted share of a "
     "mobility trigger moves by a factor of two and a half between two defensible definitions of a single trial. "
     "On the command side, {N['a3_ho']} of {N['ho_all']} handover commands are temporally associated with an A3 report "
     f"within two seconds, resolving to {N['conv_mult_own']} unique episodes under one-to-one pairing."),
      ("The per-profile breakdown, also in Table 5.12, is where the substantive result survives, and "
     "it survives more clearly under the stricter unit. The dominant mobility profile—+1 dB "
     "offset, 1 dB hysteresis, 320 ms time-to-trigger, 240 ms report interval—is not followed by a command on "
     f"{N['conv_dom_declined_1to1']:.1%} of its episodes one-to-one, against "
     f"{N['conv_mon_declined_1to1']:.1%} for the dominant negative-offset configuration. Under the "
     f"loose unit the same pair reads {N['conv_dom_declined']:.1%} against "
     f"{N['conv_mon_declined']:.1%}: the separation is present either way, and the strict unit "
     "widens it, because the negative-offset configurations turn out to be followed by almost no commands at all. "
     "That is what characterizes them empirically as neighbour-monitoring rather than mobility triggers."),
      ("The conclusion for the literature is therefore not a single conversion percentage. It is that a study "
     "using A3 reports as proxy labels for handovers must state its unit, must not let overlapping "
     "trigger episodes claim the same command, and must separate the mobility configuration from the "
     "rest before drawing conclusions about handover intent.")],
492: DELETE,
# 5.11 negative results
507: "5.11 Negative results and the cost of the audit",
508: ("Five approaches were implemented in good faith, measured, and did not help; they are reported "
     "because a claim that the formulation and the protocol are what matter is credible only if the "
     "alternatives were tried. Table 5.15 lists them."),
511: ("Table 5.15 lists them and Figure 5.19 sets each against the configuration that was retained. "
     "The dedicated ping-pong classifier is the most informative: ping-pong is highly structured at "
     "the population level, as Section 5.10 shows, and yet whether a particular transfer will be "
     "reversed is close to unpredictable from these inputs, which is consistent with it depending on "
     "the state of the target cell that the handset does not observe."),
512: [("The domain-adaptation null is consistent with published benchmarking rather than indicative "
     "of a faulty implementation. Ismail Fawaz et al. "
     f"[{N['ref_fawaz']}] benchmark nine unsupervised domain-adaptation algorithms over twelve "
     "time-series datasets and find several published methods performing worse than source-only "
     "training, with the adaptation technique rather than the backbone driving the outcome; the "
     "result here has the same shape. The same caution applies to the sequence-model arms: this is "
     "a four-session corpus, and the finding is that these architectures do not help on it, not "
     "that they cannot help on this task. Recent work is actively developing exactly those "
     f"architectures for handover prediction on larger corpora [{N['ref_kabeer']}], "
     f"[{N['ref_mandapati']}], and nothing measured here contradicts it."),
    ("Aggregate metrics can hide whether a model has learnt something or is exploiting an "
     "artefact, and this thesis reported none of the individual cases until a reviewer observed "
     "that the only trace it showed anywhere was the idealised schematic of Figure 1.1. Table 5.16 "
     f"and Figure 5.20 correct that. Nine rows were drawn uniformly at random, under a fixed seed, "
     "from the out-of-fold predictions at the operating point of Section 5.5: three the model "
     "scored above threshold with a command following within one second, three it scored above "
     "threshold with no command following, and three it scored below threshold with a command "
     "following. Nothing was chosen for being illustrative, which is the point of showing them."),
    ("Three things are visible in them that no aggregate reports. The first is that real traces do "
     "not look like Figure 1.1: the serving and best-neighbour curves cross repeatedly within the "
     "window, dip several decibels between consecutive seconds, and recross without a command "
     "being issued, which is the measured counterpart of the finding in Section 5.9 that most A3 "
     "trigger episodes are declined. The schematic is a statement of the entry condition, not a "
     "picture of the data, and should be read as such. The second is that the false alarms are not "
     "arbitrary: in the drawn cases the model alarms where a neighbour has genuinely overtaken the "
     "serving cell and the network then does not hand over, so the errors sit on the decision the "
     "network itself declines to make rather than on noise. The third is that the misses are "
     "quiet-looking rows whose command arrives with no visible precursor in the radio trace at all, "
     "which is consistent with the modest ceiling reported throughout Chapter 5 rather than with a "
     "model that could be repaired by tuning."),
    ("These are nine cases and are offered as such. They cannot establish a rate and are not used "
     "to; the rates are in Table 5.3 and Appendix A. What they establish is that the model's errors "
     "have a structure a reader can inspect, which is a claim that could not be checked at all "
     "before they were shown.")],
513: ("The largest negative result in this thesis is the alignment audit of Section 5.1, and it is "
     "recorded here as such: the strongest configuration this project measured — AUPRC "
     f"{N['lag0_auprc1']:.3f} at one second — was an artefact of a one-second misalignment between "
     "the measurement grid and the event clock, and the corrected figure is "
     f"{N['auprc1']:.3f}. Retaining that result would have produced a more impressive thesis and a "
     "false one."),
# 5.12 objectives
518: ("Objective O1 is met in a qualified form: the next handover command is predicted one second "
     f"ahead at AUPRC {N['auprc1']:.3f} against a {N['prev1']:.1%} floor ({N['lift1']:.1f}x) and "
     f"AUROC {N['auroc1']:.3f}, from quantities a handset observes strictly before the prediction "
     f"instant, against AUPRC {N['a3_auprc1']:.3f} for the rule presently deployed. The qualification "
     "is that this is a modest signal, well short of what an uncorrected protocol reports on the same "
     "data."),
519: ("Objective O2 is met, and the answer is larger than the modelling differences the thesis "
     f"reports. Between the leakiest and the soundest protocol the one-second AUPRC of the primary "
     f"model moves by {N['infl_row_lgbm']:.0f} %, and a one-row misalignment moves it by "
     f"{N['lag_effect_pct']:.0f} % — both larger than the gap between the best and worst learner "
     "under a fixed protocol."),
520: ("Objective O3 is met in part. Coherence is exact by construction, and the guarantee is "
     "reported with the alarm rate it costs, the feasibility floor set by the number of calibration "
     "units, and the measured behaviour in the non-exchangeable regime. The part that is not met is "
     "the estimand: the bound certified here is over rows, and the per-handover bound the objective "
     f"asks for cannot be certified at one second on a one-hertz grid at all, because "
     f"{N['crc_unresolvable1']:.1%} of commands have no usable row in their lead window. It becomes "
     "expressible at three seconds, at an alarm rate near one second in two. The second part that "
     "is not met is exchangeability itself: calibrating on one campaign and certifying another is "
     f"the deployment regime, and there the bound holds in {N['crc_cross_ok20']:.0%} of the "
     f"{N['crc_rotations']} rotations rather than in all of them. Both failures are properties of "
     "the setting rather than of the procedure, and the objective is reported as partly met on that "
     "basis rather than restated to fit what was achieved."),
521: ("Objective O4 is met at two distances: leave-one-campaign-out AUROC between "
     f"{N['loco_auroc_min']:.3f} and {N['loco_auroc_max']:.3f}, including "
     f"{N['loco_auroc_highway']:.3f} on the highway corridor, and AUROC "
     f"{N['ext_ours_to_pub_auroc']:.3f} on an independent collection on the same network."),
522: ("Objective O5 is met in corrected form. The single strongest lesson is methodological — the "
     "alignment audit — and the substantive one is that the quantity Event A3 thresholds on is a weak "
     f"predictor of imminence (AUROC {N['gap_auroc']:.3f} alone), while no single available feature "
     "replaces it."),
# 5.13 threats
524: [("Six threats bear on the results above and are stated here rather than deferred."),
    ("Post-hoc windows. Four windows in this thesis were chosen after the data were collected: the "
     "180-second block that defines the grouped protocol, the 60-second purge around a blocked test "
     "fold, the ten-second cutoff separating quiet rows from rows inside a burst, and the two-second "
     "blank after a handover command. Each is a degree of freedom, and a result that turns on one of "
     "them is not a result. Table A.5 varies all four. The inflation from random-block splitting is "
     f"positive at every block length from {N['sens_block_range']} seconds, ranging from "
     f"{N['sens_block_min']:.1f} % to {N['sens_block_max']:.1f} %, so the conclusion of Section 5.3 "
     "holds while its exact magnitude does not. The quiet-over-burst ordering of Section 5.8 holds "
     f"at every cutoff from five to twenty seconds. The lift is stable across the blank window, "
     f"running from {N['sens_blank_lift_min']:.1f}x to {N['sens_blank_lift_max']:.1f}x as it widens "
     "from zero to five seconds, and rises slightly as the blank grows, so the two-second choice is "
     "conservative rather than favourable. The purge is the exception and is reported as one: the "
     f"blocked protocol sits between {N['sens_purge_min']:+.1f} % and {N['sens_purge_max']:+.1f} % "
     "of leave-one-campaign-out depending on the purge, which straddles zero, so the small gap "
     "between blocked cross-validation and the campaign-level protocol is not established by this "
     "data in either direction. The two large inflations are robust; the small one is not, and "
     "Section 5.3 should be read accordingly.")],
525: ("Scale and coverage. The evidence is four continuous sessions on one operator over four days. "
     "Four units bound what any paired test can say, and the block bootstrap intervals quoted "
     "alongside are conditional on those four sessions. A second operator would test what a second "
     "corridor cannot."),
526: ["Residual alignment risk. The audit of Section 5.1 establishes that a one-row lag is necessary "
      "and, on the evidence of the isolated-handover baseline, sufficient on this export. It does not "
      "establish the exact semantics of the XCAL row timestamp, which the vendor documentation does "
      "not state; a native-rate export would settle it. Every result here is therefore conservative "
      "by one sample period rather than exact. The mechanism of Section 5.1 narrows the residual risk "
      "but does not remove it: it accounts for the sign and the intra-second gradient of the effect, "
      f"and for the {N['mech_resid_share']:.0%} of handovers where the export refreshes its "
      "serving-cell field a row late, but a one-row lag remains a conservative repair chosen without "
      "vendor confirmation of the write rule.",
      "Instrument ceiling. The one-hertz export caps event-level detection at "
      f"{N['ceiling1']:.1%} at one second, which bounds every event-level figure in Section 5.2."],
527: DELETE,
528: ("Comparator availability. The nearest comparator method on this network is a reinforcement-"
     f"learning formulation of the handover decision [{N['ref_shafi_rl']}] developed in the same "
     "department on the same operator and instrument, whose released dataset is the one used in "
     "Section 5.7. It was reimplemented from its method description and scored as a predictor, where "
     "it proved close to uninformative — which is expected, since it optimises a decision rather than "
     "forecasting an event. The comparison is reported in Section 5.11 rather than as a headline, and "
     "its provenance is stated here because the two works share an institution and a supervisor."),

# ---------------------------------------------------------------- CH 6 / 7
545: ("It involves conflicting technical requirements whose trade-offs must be resolved quantitatively "
     "rather than asserted. The clearest is the warning trade-off of Section 5.5: an earlier and more "
     f"sensitive warning is more useful to the network and also more expensive, and the frontier from "
     f"a 20 % certified miss rate at a {N['crc_alarm20']:.0%} alarm rate to a 5 % target at "
     f"{N['crc_alarm05']:.0%} is measured rather than assumed."),
546: ("It is not amenable to a standard procedure. The evaluation protocol the comparator literature "
     "treats as standard is demonstrably wrong on this data, and Section 5.3 measures by how much; "
     "so is the implicit assumption that a measurement row describes the interval before its own "
     "timestamp, which Section 5.1 measures."),
550: ("Environmental and sustainability. The environmental argument made here is confined to what was "
     f"measured. Section 5.10 shows that {N['pingpong']:.1%} of the handovers observed returned to "
     "the cell just vacated within fifteen seconds, and that the phenomenon is concentrated in "
     "intra-carrier transfers under one dominant configuration profile. Each such pair is signalling "
     "work, radio resource occupancy and processing load expended for no change in serving cell. "
     "Identifying that fraction is a precondition for reducing it. This thesis does not claim a "
     "measured energy saving, because no such measurement was made."),
552: [("Ethical and legal. Four ethical considerations arose and were handled explicitly. Measurements "
     "were collected from the authors' own handsets and subscriptions, so no third-party user data "
     "was recorded and no subscriber identifiers appear in the dataset. The operator is not named, "
     "because the purpose of reporting its configuration is scientific rather than comparative. "
     "Negative results were reported rather than omitted, including the one in Section 5.1 that "
     "removed this project's most impressive number. And the dataset used for the independent check "
     "in Section 5.7 was produced in the same department as this work, which is stated in the text "
     "rather than left for a reader to discover."),
      ("Declaration of competing interests. The supervisor of this thesis is a co-author of the "
     f"publicly released dataset used for the independent check of Section 5.7 [{N['ref_shafi_ds']}] "
     f"and of the comparator method reimplemented in Section 5.11 [{N['ref_shafi_rl']}]. Neither the "
     "dataset nor the comparator was modified for this work; both were taken as released, and the "
     "results involving them are reported in the same form as every other result. The relationship "
     "is declared here because the word independent, applied to a collection produced by an "
     "overlapping group on the same operator with the same instrument, would otherwise carry more "
     "weight than the evidence supports. The authors declare no other competing interest.")],
562: ("The project was conducted over two academic terms. The first term covered problem "
     "identification, the literature screening of Section 2.3 and the construction of the decoding "
     "pipeline; the second covered the four measurement campaigns of September 2026, the formulation "
     "and modelling of Chapter 4, the evaluation of Chapter 5 and the preparation of this report. The "
     "alignment audit of Section 5.1 was performed during the final revision, after all four "
     "campaigns and after the first complete draft, and it required every experiment in Chapter 5 to "
     "be re-run."),
# CH 7
568: ("This thesis set out to determine whether the next LTE handover can be forecast from the "
     "measurements a handset already reports, how far in advance, and with what guarantee. On four "
     "drive-test campaigns conducted on a live commercial network in Dhaka and Gazipur — four "
     f"continuous sessions, {N['rows_qc']:,} samples and {N['ho_all']} handover commands whose ground "
     "truth is decoded from RRC signalling — the answer is a qualified yes, and the qualification is "
     "the first finding."),
569: ("The measurement grid must be aligned to the event clock before anything else is believed. In "
     f"{N['align_t0']:.0%} of handovers the one-hertz export row stamped before the handover command "
     "already carries the post-handover serving cell. Lagging every export feature by one row moves "
     f"the one-second AUPRC of the same model from {N['lag0_auprc1']:.3f} to {N['auprc1']:.3f}, and "
     f"the dwell feature's own AUROC from {N['lag0_dwell_auroc']:.3f} to {N['dwell_auroc']:.3f}. The "
     "audit costs three lines of code and is the recommendation this thesis makes most strongly."),
570: ("The next handover is predictable, modestly. Gradient-boosted trees under a discrete-time hazard "
     f"formulation reach AUPRC {N['auprc1']:.3f} at one second against a {N['prev1']:.1%} floor "
     f"({N['lift1']:.1f}x, AUROC {N['auroc1']:.3f}) and AUPRC {N['auprc5']:.3f} at five seconds "
     f"({N['lift5']:.1f}x), against AUPRC {N['a3_auprc1']:.3f} for the implemented A3 signal-margin proxy scored "
     "as a predictor."),
571: ("The unit of splitting is worth more than the model. Against leave-one-campaign-out, random-row "
     f"splitting inflates the one-second AUPRC by {N['infl_row_lgbm']:.0f} % and random 180-second "
     f"blocks of the same session by {N['infl_chunk_lgbm']:.0f} %; the inflation is "
     "architecture-dependent, so a careless protocol reorders the leaderboard rather than merely "
     "raising it."),
572: ("A single hazard model makes the five horizons mutually consistent, and the correct comparison "
     "for that property is a monotone projection rather than raw independent classifiers. "
     f"Independent heads violate ordering on {N['viol_indep']:.1%} of rows; a cumulative-maximum "
     "projection, isotonic calibration composed with that projection, and the hazard model all reach "
     f"zero. The hazard model calibrates better than the projections that cost nothing "
     f"({N['ece1_hazard']:.3f} against {N['ece1_cummax']:.3f} at one second) and worse than the "
     f"isotonic composite ({N['ece1_isopav']:.3f}), which spends a calibration split this dataset "
     "cannot spare."),
573: ("Conformal risk control evaluated over rows yields an empirical miss rate at or below the target "
     f"within pooled 180-second blocks ({N['crc_loss20']:.3f} at α = 0.20, at a {N['crc_alarm20']:.0%} alarm rate); "
     f"calibrating on one campaign and evaluating on another, the empirical mean miss loss across campaigns was "
     f"{N['crc_cross_loss20']:.3f} at target α = 0.20 (with holdout loss falling below α in {N['crc_cross_ok20']:.0%} of "
     "rotations), though cross-campaign results remain descriptive because exchangeability is not established across "
     f"distinct environments. Over handovers, which is what the objective promised, it cannot be certified at one "
     f"second at any useful level, because {N['crc_unresolvable1']:.1%} of commands have no usable row in their lead "
     "window (under the two-second post-handover blanking policy and boundary exclusions) and are missed whatever "
     "the threshold. The evaluation policy limit, rather than model discrimination alone, is the binding constraint on "
     "what a study like this may promise."),
574: ("Two properties of the deployed network were characterised, both corrected against an earlier "
     "draft, and a third quantity was shown to have no unit-free value at all. The A3 conversion "
     f"rate reads {N['conv_rep_declined']:.0%} declined per transmitted report, "
     f"{N['conv_ep_declined']:.0%} per trigger episode when overlapping episodes may claim the same "
     f"command, and {N['conv_ep1_declined']:.0%} once each command is matched to exactly one "
     "episode; what survives every unit is the separation between the mobility configuration and "
     f"the monitoring configurations ({N['conv_dom_declined_1to1']:.0%} declined against "
     f"{N['conv_mon_declined_1to1']:.0%}). Weighted by handovers rather than by configurations, "
     f"{N['a3_pos_share']:.0%} of A3-triggered handovers fire under a positive offset. And the "
     f"ping-pong rate on one fixed set of {N['ho_all']} handover commands moves from "
     f"{N['pp_strict']:.1%} to {N['pp_any']:.1%} depending on three definitional choices that "
     "published work routinely leaves unstated."),
575: ("Underlying all of these is the most compact statement of what this work found: on drive-test "
     "data, the protocol and the timestamp semantics decide the result before the model does."),
577: ("The limits of the present study are restated here because they determine what follows from it."),
578: ("The evidence base is four continuous sessions, one operator, four days and one radio access "
     "technology. The deployed configuration is stable across all four campaigns, so the cross-regime "
     "transfer of Section 5.6 rests on a single configuration set; a second operator would test what "
     "a second corridor cannot. The one-hertz export caps event-level detection and fixes the "
     "information cut-off one sample period before the prediction instant, so every figure here is "
     "conservative by that margin and exact semantics await a native-rate export."),
579: DELETE,
580: DELETE,
582: ("Six items follow from those limits, in the order the limits dictate."),
583: ["A native-rate export and a published alignment statement. The single most valuable follow-up is "
      "a re-export of the same captures at the message rate rather than at one hertz. Section 5.1 "
      "infers the write rule from its consequences and confirms the inference on a native-rate corpus "
      "collected elsewhere; a native-rate export of these captures would replace inference with "
      "observation and would allow horizons below one second to be modelled rather than "
      "approximated.",
      "More sessions, not more blocks. Eight to ten independent sessions across days, times and "
      "routes would make a paired test over units informative, tighten the conformal feasibility "
      "floor and give the block bootstrap something to be conditional on."],
584: ("A second operator. Four days on one network is sufficient for a careful study and insufficient "
     "to claim generality; a second operator would also break the single-configuration limitation, "
     "since the offsets reported in Section 3.5 are one operator's choice rather than a property of "
     "LTE."),
585: DELETE,
586: ("An experimental test of the time-to-trigger. Section 5.10 withdraws the claim that the "
     "time-to-trigger is the lever governing ping-pong, because the configuration set does not vary "
     "anywhere in this data and the claim is therefore unsupported rather than merely uncertain. It "
     "remains the most plausible lever on theoretical grounds, and varying it under controlled "
     "conditions while measuring the resulting return rate is the direct test. Such an experiment "
     "would also supply the treatment variation that the causal design above requires, and would "
     "break the confounding between speed, route and cell density that four campaigns cannot."),
587: ("Release of code and data. The protocol and alignment arguments of Sections 5.1 and 5.3 are "
     "claims about how results in this field should be produced, and such claims are only worth "
     "making if others can reproduce and contest them — Table 2.1 grades 22 published models on "
     "exactly that property, so this work's own two rows in it have to say something specific. On "
     "acceptance, the alignment audit, the four evaluation protocols, the feature construction and "
     "the table-generation pipeline are released as a versioned archive, together with the derived "
     "row-level frames and the decoded event streams. The raw vendor logs are not redistributable, "
     "which is stated rather than elided; the derived frames are sufficient to reproduce every "
     "number in Chapter 5."),
589: ("The contribution of this work is not a new learning algorithm, and it is worth closing by "
     "saying so directly. Gradient boosting, discrete-time survival analysis and conformal risk "
     "control are all established methods, and this thesis is explicit throughout about where each "
     "came from. What it offers is measurement-grade ground truth decoded from signalling, a "
     "formulation that makes multi-horizon outputs coherent by construction, an evaluation protocol "
     "whose unit is defended and whose alternatives are measured, a guarantee whose exchangeability "
     "assumption is tested in both directions, and an alignment audit that removed this project's own "
     "most impressive result. On the evidence of the protocol audit in Section 2.3, the parts of that "
     "list the field most consistently omits are the least novel ones."),
}
